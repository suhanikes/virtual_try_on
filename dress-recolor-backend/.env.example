PORT=4000
# Frontend origin for CORS (Vite default)
CORS_ORIGIN=http://localhost:5173
#
# Python ML service base URL (FastAPI on port 8000)
ML_SERVICE_URL=http://127.0.0.1:8000
#
# Optional: override for mask preview URLs returned to frontend.
# If omitted, backend returns http://localhost:${PORT}
# PUBLIC_BASE_URL=http://localhost:4000

