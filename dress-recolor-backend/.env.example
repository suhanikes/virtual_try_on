PORT=4000
# Frontend origin for CORS (Vite default)
CORS_ORIGIN=http://localhost:5173
# Hugging Face token with Inference Providers permission
HF_TOKEN=
# Optional overrides
HF_SEGMENTATION_MODEL=mattmdjaga/segformer_b2_clothes
HF_INFERENCE_API_BASE=https://router.huggingface.co/hf-inference/models
#
# Optional: override for mask preview URLs returned to frontend.
# If omitted, backend returns http://localhost:${PORT}
# PUBLIC_BASE_URL=http://localhost:4000

